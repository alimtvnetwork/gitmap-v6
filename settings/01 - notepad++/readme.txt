export or unzip to "%appdata%" or "C:\Users\{user}\AppData\Roaming"

Usage:
  gitmap install npp             # Install Notepad++ and sync settings
  gitmap install npp-settings    # Sync settings only
  gitmap install install-npp     # Install Notepad++ only (no settings)
